# Avology V2 Bug and Risk Register

| ID | Area | Role | Screen/Flow | Problem | Severity: Critical/High/Medium/Low | Likely Cause | Related files | Suggested Fix Direction | Fix Cost: Small/Medium/Large/Unknown |
|---|---|---|---|---|---:|---|---|---|---:|
| R-001 | Release Baseline | All | Whole project | Working tree is not clean: two modified service files and one untracked migration. This version is unsafe to freeze as final without deciding what belongs in the baseline. | High | Recent task/photo changes not committed/normalized. | `src/services/careScheduleService.ts`, `src/services/careTaskService.ts`, `supabase/migrations/017_hotfix_requires_photo_columns.sql` | Review diff, decide whether to keep, run DB tests, then commit or discard intentionally. | Small |
| R-002 | Database Migration | Owner/Worker | Schedule/task proof photo | `requires_photo` exists in service selects and migration 016, but untracked hotfix 017 repeats the column add. This suggests schema/cache mismatch was encountered or expected. | High | Supabase schema cache or migration application uncertainty. | `src/services/careScheduleService.ts`, `src/services/careTaskService.ts`, `supabase/migrations/016_add_requires_photo_and_task_proof.sql`, `supabase/migrations/017_hotfix_requires_photo_columns.sql` | Apply migrations to target DB, refresh schema cache, run DB tests, remove duplicate/unneeded hotfix if not required. | Small/Medium |
| R-003 | Access Control | Worker | Rejected/removed worker login and deep-link access | Existing regression doc says dedicated rejected/removed screens were not stable as initial login route and must be retested. | High | Route guard recovery flow around inactive memberships is complex. | `docs/testing/batch-3-route-guard-regression.md`, `src/utils/routeGuard.ts`, `app/(onboarding)/_layout.tsx`, `app/(worker)/_layout.tsx` | Execute manual route-guard matrix for pending/rejected/removed/active users; harden redirects if any operational route is reachable. | Medium |
| R-004 | Storage/RLS | Owner/Worker | Photo attachments | Photo features depend on private bucket policies, storage-path constraints, and signed URLs. Typecheck cannot verify Supabase Storage policy behavior. | High | Complex RLS/policy chain across `photo_attachments` and `storage.objects`. | `src/services/photoAttachmentService.ts`, `supabase/migrations/013_create_photo_attachments_and_storage.sql`, `014_refine_tree_main_photo_policies.sql`, `015_refine_operational_report_photo_policies.sql`, `016_add_requires_photo_and_task_proof.sql` | Run media regression checklist against final Supabase project with owner and worker accounts. | Medium |
| R-005 | Worker Task Realization | Worker | Complete task with required proof photo | Completing a task and uploading proof are separate steps. Code attempts rollback if proof upload fails, but a failed rollback leaves status needing manual inspection. | Medium | Mobile network/storage operation occurs after activity creation. | `app/(worker)/worker/tasks/[taskId].tsx`, `src/services/careTaskService.ts`, `src/services/photoAttachmentService.ts` | UAT required-photo failure paths; consider DB-side transaction/RPC for activity+proof metadata if instability appears. | Medium/Large |
| R-006 | Operational Reports | Worker | Create report with photo | Report is created before optional photo upload. If photo upload fails, report remains without photo. | Medium | Multi-step client workflow. | `src/components/operational-report-screen.tsx`, `src/services/operationalReportService.ts`, `src/services/photoAttachmentService.ts` | Accept as optional-photo behavior or add clearer user recovery/upload retry. | Small/Medium |
| R-007 | Condition Reports | Owner/Worker | Create condition report with photo | Condition report is created before optional photo upload. If upload fails, condition still changes and no photo exists. | Medium | Multi-step client workflow plus trigger updates tree condition. | `src/components/tree-condition-report-screen.tsx`, `src/services/conditionReportService.ts`, `src/services/photoAttachmentService.ts`, `supabase/migrations/006_create_indexes_and_triggers.sql` | Decide whether photo is optional. If required for thesis/UAT, add stricter flow or retry mechanism. | Medium |
| R-008 | Dashboard | Owner | Owner dashboard | Dashboard uses many independent queries and client-side SOP due calculation; partial failures can block the whole summary and performance may degrade with many SOPs. | Medium | No dashboard RPC/view; repeated per-SOP calls. | `src/services/dashboardService.ts`, `app/(owner)/owner/index.tsx`, `docs/source-of-truth/decision-log.md` | Run performance UAT with realistic data; consider one aggregate RPC if slow or fragile. | Medium |
| R-009 | Dashboard | Worker | Worker dashboard | Worker task counts depend on date comparisons using device-local date and direct table counts. | Medium | Client-side today calculation and timezone behavior. | `src/services/dashboardService.ts`, `app/(worker)/worker/index.tsx` | Verify date behavior in Asia/Jakarta and target test devices; consider server-side date if mismatch occurs. | Small/Medium |
| R-010 | Tree History | Owner/Worker | Tree detail history | Condition report `sourceId` is inferred by matching condition title, actor, and timestamp because `tree_history_view` does not expose source ids. | Medium | View omits source record id for union rows. | `src/services/historyService.ts`, `supabase/migrations/005_create_growth_phase_and_history_view.sql` | Add source id to view in future migration if history item linking becomes important. | Medium |
| R-011 | Harvest Requirement | Owner/Worker | Growth phase/harvest | Automatic harvest estimation is not implemented. The project docs intentionally scope it as monitoring, but thesis text must not promise prediction/estimation beyond this. | High | Scope decision removed prediction as unrealistic. | `docs/source-of-truth/04-mvp_scope.md`, `docs/source-of-truth/decision-log.md`, `src/services/growthPhaseService.ts`, `src/components/growth-monitoring-screen.tsx` | Align thesis wording and UAT cases to "growth phase tracking as harvest monitoring aid." | Small |
| R-012 | Worker Assignment | Owner | Create schedule from SOP | Service enforces exactly one worker for SOP schedule even type accepts `assignedWorkerIds`. | Low | Implementation simplified to one task/worker per schedule. | `src/services/careScheduleService.ts`, `app/(owner)/owner/sops/[sopId]/schedule.tsx` | Keep if MVP expects one worker; otherwise expand RPC/UI to multiple task rows. | Medium |
| R-013 | Testing Evidence | All | Final thesis acceptance | DB test scripts and black-box/UAT plans exist, but this audit did not find executed result artifacts proving the final app works end-to-end. | High | Plans exist without captured run output/results. | `scripts/db-tests/*`, `docs/source-of-truth/16-black-box-plan-testing.md`, `docs/source-of-truth/17-uat-plan.md` | Run DB tests, run black-box/UAT, save results in docs before final thesis freeze. | Medium |
| R-014 | Media QA | Owner/Worker | Camera/gallery flows | Media regression checklist entries are marked Manual Pending. | Medium | Device permissions and image picker behavior require manual/device testing. | `docs/testing/media-regression-checklist.md`, `src/lib/media.ts`, `src/services/photoAttachmentService.ts` | Execute camera/gallery permission, size, upload, preview, delete checks on target devices. | Medium |
| R-015 | Profile/Actor Labels | Owner/Worker | Reports/history/tasks | If member profile RPCs fail or RLS changes, UI falls back to generic names such as "Pengguna tidak tersedia." | Low | Actor display data loaded separately from main records. | `src/services/memberService.ts`, `src/services/historyService.ts`, `src/components/operational-report-screen.tsx`, `app/(owner)/owner/tasks/[taskId].tsx` | Include display-name checks in UAT; ensure RPC grants/policies are applied. | Small |

